function getBeer(beer) {
    return {
        name: beer.name,
        price: beer.price,
        rating: beer.rating,
        description: beer.description,
        image: beer.image,
        id: beer.id,
    }
}